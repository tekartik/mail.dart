name: tekartik_mail
description: Abstract email api
version: 0.1.2
publish_to: none

environment:
  sdk: ^3.5.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: '>=2.0.0'
  test: '>=1.21.0'
