name: tekartik_mail
description: Abstracy email api
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.1.5

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: '>=2.0.0'
  test: '>=1.21.0'
